{
  "name": "carlson-gracie-nz",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "studio:dev": "npm --prefix studio run dev",
    "studio:build": "npm --prefix studio run build",
    "studio:deploy": "npm --prefix studio run deploy",
    "cms:import": "node cms/import-content.mjs",
    "test:cms": "node --test cms/*.test.mjs"
  },
  "devDependencies": {
    "cheerio": "^1.2.0",
    "vite": "^8.2.1"
  },
  "dependencies": {
    "lenis": "^1.3.26"
  }
}
