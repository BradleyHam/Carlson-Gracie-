node_modules/
dist/
.DS_Store
.vercel

# Raw/working footage lives at the repo root and never gets committed.
# The site only needs the compressed copies in assets/, which stay tracked.
/*.mp4
/*.mov
/*.MP4
/*.MOV
/*.avi
/*.mkv
