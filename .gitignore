node_modules/
dist/
.DS_Store
.vercel

# Video is ignored everywhere by default, so stray source footage can never be
# committed by accident. Only the files the site actually serves are allowed.
*.mp4
*.mov
*.MP4
*.MOV
*.avi
*.mkv
!assets/hero-reel.mp4
!assets/hero-reel-mobile.mp4
!assets/hero-film.mp4
!assets/technique.mp4

# original full-size photography stays local
assets/images to add/

# pre-optimisation originals: kept locally so any asset can be re-encoded
assets/_original/

# Published inline films
!assets/live-class.mp4
!assets/kids-rolling.mp4
!assets/womens-rolling.mp4
!assets/locations-train.mp4
!assets/locations-teach.mp4
!assets/locations-belong.mp4

# Local CMS credentials and generated Studio files
.env
.env.*
!.env.example
studio/.sanity/
studio/dist/
